# 测试计划与测试用例

## 范围

覆盖健康检查、模块装配、知识 CRUD、问答证据、缓存、模型降级、订单事实、会话历史、工单、客户、持久化、认证、RBAC、租户隔离和审计。

| 编号 | 场景 | 期望 |
|---|---|---|
| TC-01 | 无证据问答 | 拒答且无虚构引用 |
| TC-02 | 订单物流查询 | 返回业务系统事实及运单号 |
| TC-03 | 跨租户查询 | 403/404 |
| TC-04 | 模型 503 | 降级返回证据原文 |
| TC-05 | 转人工到解决 | 工单和会话状态同步 |
| TC-06 | 服务重启 | 知识、会话仍存在 |
| TC-07 | 错误密码/过期令牌 | 401 |
| TC-08 | viewer 写客户 | 403 |
| TC-09 | 管理员写操作 | 生成审计日志 |

执行：`uv run python -m pytest -q`。当前结果为 `29 passed`。已建立 180 条、12 类电商客服评测集并完成字段完整性与唯一性校验；已对 Docker Compose 环境（PostgreSQL、Redis、HTTP OMS、mock 模型）执行 100 请求/10 并发压测，成功率 100%，吞吐 73.89 RPS，P50 128.54 ms，P95 169.19 ms，P99 188.25 ms。报告位于 `evaluation/reports/`。真实模型评测、提示词注入、依赖漏洞、渗透、数据恢复、浏览器兼容和 72 小时稳定性测试仍须在生产候选环境执行。
